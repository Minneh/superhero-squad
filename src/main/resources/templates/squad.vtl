#if ($squad)
<h1>$squad.getName() heroes: </h1>

<ul>
  #foreach($hero in $squad.getHeroes())
  <li>$hero.getName()</li>
  #end
</ul>
#else
<em><p>Oops! We can't seen to find that squad.</p></em>
#end

<a href="/squads/$squad.getId()/heroes/new">Add a new hero</a>
<a href="/squads">All squads</a>
